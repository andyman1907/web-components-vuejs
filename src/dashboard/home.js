const axios = require("axios");

module.exports = {
   /*  getListComponent: async () => {
        const data = await axios.get("components.json");
        return data;
    } */
    getListComponent:"hellos"
}
